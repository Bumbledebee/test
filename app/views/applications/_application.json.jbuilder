json.extract! application, :id, :name, :publisher_id, :platform_id, :fyberapplicationid, :dashboardlink, :ticketlink, :buildlink, :created_at, :updated_at
json.url application_url(application, format: :json)
